# predictions

This folder stores generated outputs from the local/offline product console.

## Typical files
- `*_prediction.json` — serialized event + probability summary
- `*_windows.csv` — window-level probabilities and predictions
- `*_frames.csv` — frame-level reconstructed data used for playback/debugging

These files are useful for debugging, reproducibility, and report preparation.
